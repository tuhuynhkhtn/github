@using MVCQLBH.Models

@model IList<Category>

<div class="list-group">
    @foreach (var c in Model)
    {
        <a href="@Url.Action("GetListByCategory", "Product", new { id = c.CatID})" class="list-group-item">
            @c.CatName
        </a>
    }
</div>